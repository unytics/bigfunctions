type: function_py
category: notify
author:
  name: Guillaume Pivette from Neoxia
  url: https://www.linkedin.com/in/guillaume-pivette/
  avatar_url: "https://cdn-images-1.medium.com/v2/resize:fit:92/1*jHdQzX82eU5lyjBYp63NqQ@2x.png"
description: |
    Sends `message` to a slack channel.

    > To get the `webhook_url` for a channel, follow this [Slack Documentation](https://api.slack.com/messaging/webhooks)
    > and then copy the given webhook url which should look like: `https://hooks.slack.com/services/T00000000/B00000000/XXXXXXXXXXXXXXXXXXXXXXXX`.
arguments:
  - name: message
    type: string
  - name: webhook_url
    type: string
output:
  name: response
  type: string
examples:
  - description: "Send a message to channel 'I am a hero!' of BigFunctions Slack"
    arguments:
      - '"Hello 👋 from bigfunctions!"'
      - '"https://hooks.slack.com/services/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"'
    output: |
      {
        "ok": true
      }
    region: ALL
code: |
  import requests
  response = requests.post(
      webhook_url,
      headers={'Content-Type': 'application/json; charset=UTF-8'},
      json={'text': message}
  )
  if not response.ok:
      raise Exception(response.text)
  return response.text
requirements: |
  requests
quotas:
  max_rows_per_query: 10
